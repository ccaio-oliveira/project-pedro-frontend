import styled from 'styled-components';
import { TD, Tabela } from '../../../global.styles';

export const TableRelatoriosAgenda = styled(Tabela)`
    width: 100%;
    border: none;
`;

export const TDRelatoriosAgenda = styled(TD)`
    width: 50%;
    text-align: center;
`;
