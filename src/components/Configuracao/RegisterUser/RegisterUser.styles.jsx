import styled from 'styled-components';
import { FormGroup, FormLabel, InputData, SelectInput } from '../../../global.styles';
import { ErrorP } from '../../../pages/Login/Login.styles';

export const RegisterUserContainer = styled.div`
    width: 90%;
    display: flex;
    flex-direction: column;
    align-items: center;
`;

export const RegisterUserFormGroup = styled(FormGroup)`
    
`;

export const RegisterUserLabel = styled(FormLabel)`
        
`;

export const RegisterUserInput = styled(InputData)`
        
`;

export const RegisterUserSelect = styled(SelectInput)`

`;

export const RegisterUserErrorP = styled(ErrorP)`
    margin: 0;
    font-size: 0.8rem;
`;