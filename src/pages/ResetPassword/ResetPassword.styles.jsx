import { styled } from 'styled-components';
import { ErrorP } from '../Login/Login.styles';

export const ErrorReset = styled(ErrorP)`
    font-size: 0.9rem;

    width: 20rem;

    margin-top: 0;
    margin-bottom: 10px;
`;